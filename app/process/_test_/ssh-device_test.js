var expect = require("chai").expect;

var SSHDevice     = require("./../ssh-device");

describe("SSHDevice", function () {

  var device;

  beforeEach(function () {
    device = new SSHDevice();
  });

  it("should create sequest instance");

  it("should transmit data");

  it("should process multiple commands in series");

});